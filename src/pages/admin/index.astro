---
// The manual Editorial Desk has been retired (content now flows through the
// agents + URL intake + pending queue). Land /admin on the Pending Queue.
export const prerender = false;
return Astro.redirect("/admin/queue/", 302);
---
